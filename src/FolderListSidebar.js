import React, { Component } from 'react'
// PROPS PASSED IN:

class FolderListSidebar extends Component {
  render() {
    console.log(this.props);
    // const folders = Object.keys(this.props.folders).map(folder => {});

    return (
      <div>
        <ul>x</ul>
        <ul>FolderListSidebar item 2</ul>
        <ul>FolderListSidebar item x</ul>
      </div>
    )
  }
}

export default FolderListSidebar
